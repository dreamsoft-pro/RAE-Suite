FROM python:3.14-rc-slim
WORKDIR /app
COPY . .
RUN pip install fastapi uvicorn httpx pydantic pyyaml
ENV PYTHONPATH=/app/src:/app/packages/rae-core/src
CMD ["uvicorn", "rae_quality.main:app", "--host", "0.0.0.0", "--port", "8010"]
